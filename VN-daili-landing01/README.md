# NodeShort Landing (VI)
- Deploy: Vercel → New Project → import repo → Framework: Other/Static
- Replace `og:url` and `<link rel="canonical">` with your live domain after first deploy.
- Test Pixel: Events Manager → Test events → open the page.
